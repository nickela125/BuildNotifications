<Page x:Class="BuildNotifications.BuildList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BuildNotifications"
      mc:Ignorable="d" 
      d:DesignHeight="350" d:DesignWidth="525"
      Title="BuildList"
      DataContext="{Binding BuildList, Source={StaticResource Locator}}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <TextBlock FontWeight="Bold" FontSize="14" Text="Builds" Height="20" Margin="10" />
        <Button 
            Grid.Row="2"
            Content="Manage Accounts..."
            Command="{Binding ManageAccountsCommand}"
            Margin="10"
            Width="150"
            HorizontalAlignment="Right"
            />
    </Grid>
</Page>
